function [grammar] = scaleRulesProb (grammar,rulesProb)

for i=1:length(grammar.rules.connections)

index=grammar.rules.connections(1);

idx=cell2mat(index)

sameSum=sum(rulesProb(idx))
rulesProb(idx)=rulesProb(idx)/same

end
end